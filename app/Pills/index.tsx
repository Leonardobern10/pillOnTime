import { Text } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";

export default function Pills({ navigation }: any) {
  return (
    <SafeAreaView>
      <Text>Pagina Pills</Text>
    </SafeAreaView>
  );
}
