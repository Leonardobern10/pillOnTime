import { Text } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";

export default function Add({ navigation }: any) {
  return (
    <SafeAreaView>
      <Text>Pagina Add</Text>
    </SafeAreaView>
  );
}
