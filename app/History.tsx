import { Text } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";

export default function History({ navigation }: any) {
  return (
    <SafeAreaView>
      <Text>Pagina History</Text>
    </SafeAreaView>
  );
}
